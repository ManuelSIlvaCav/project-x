import { type Metadata } from "next";

export const metadata: Metadata = {
  title: "Company Jobs",
};

export default function JobsPage() {
  return (
    <div>
      <h1>Jobs</h1>
    </div>
  );
}
