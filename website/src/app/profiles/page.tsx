import { type Metadata } from "next";

export const metadata: Metadata = {
  title: "Profiles Page",
};

export default function ProfilesPage() {
  return (
    <div>
      <h1>profiles</h1>
    </div>
  );
}
